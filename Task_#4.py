Provider_Name= input("Write the names of providers (Separate each name with a comma): ")
#Offers= float(input("Write the cost of offers (Separate each amount with a comma:) "))

input_provider = Provider_Name.split()
print(input_provider)

